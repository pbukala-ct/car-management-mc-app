query FetchBundles($limit: Int!, $offset: Int!) {
  customObjects(container: "shop-the-look", limit: $limit, offset: $offset, sort: ["lastModifiedAt desc"]) {
    count
    total
    offset
    results {
      id
      key
      version
      value
      lastModifiedAt
    }
  }
}
